class Keyboard {

    LEFT = false;  
    RIGHT = false;
    UP = false;
    DOWN = false;
    SPACE = false;
    THROW = false;
    ENTER = false;

/**
 * Initializes the Keyboard class.
 * Sets up the event listeners for keydown and keyup events.
 * @returns {void}
 */
  constructor() {
    this.name = 'Keyboard';
  }
}